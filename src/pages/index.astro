---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceCard from '../components/ServiceCard.astro';
import StatCard from '../components/StatCard.astro';
import CaseStudyCard from '../components/CaseStudyCard.astro';
import Cta from '../components/Cta.astro';
import Icon from '../components/Icon.astro';
import { SITE } from '../lib/site';

const allServices = (await getCollection('services')).sort((a, b) => a.data.order - b.data.order);
const pillars = [
  { name: 'Web Development', services: allServices.filter((s) => s.data.pillar === 'Web Development') },
  { name: 'SEO & AI Search Visibility', services: allServices.filter((s) => s.data.pillar === 'SEO & AI Search Visibility') }
];
const featuredStudies = (await getCollection('caseStudies'))
  .filter((s) => s.data.featured)
  .sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf());

const description =
  'Affordable website design and development plus full-service SEO and AI search visibility (AEO/GEO). We handle the site and the visibility so you can stay focused on your craft.';

const orgSchema = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  name: SITE.name,
  legalName: SITE.legalName,
  url: SITE.url,
  email: SITE.email,
  telephone: SITE.phone,
  description,
  address: {
    '@type': 'PostalAddress',
    addressLocality: SITE.addressLocality,
    addressRegion: SITE.addressRegion,
    addressCountry: 'US'
  },
  sameAs: [SITE.social.linkedin, SITE.social.x],
  areaServed: 'US',
  foundingDate: SITE.founded
};
---

<BaseLayout title="Websites, SEO/GEO & Leads" description={description} structuredData={orgSchema}>
  <section class="border-b border-line bg-paper">
    <div class="mx-auto grid max-w-6xl gap-10 px-6 py-20 lg:grid-cols-[3fr_2fr] lg:items-center">
      <div>
        <h1 class="text-4xl font-semibold leading-tight text-ink sm:text-5xl">
          A website that actually brings you business. Built affordably, found easily.
        </h1>
        <p class="mt-6 max-w-xl text-lg text-slate">
          Websites shouldn't be a five-figure expense — they're a revenue channel and proof you're legitimate. {SITE.name} builds the site and handles the SEO/GEO work behind it, so you can stay focused on your craft while we take care of your visibility, your leads, and your conversions.
        </p>
        <div class="mt-8 flex flex-wrap gap-4">
          <a href="/contact" class="inline-flex items-center gap-2 rounded-md bg-signal-cta px-6 py-3 font-medium text-ink hover:bg-signal-bright">
            Get a free site &amp; visibility review
            <Icon name="arrow-right" class="h-5 w-5" />
          </a>
          <a href="/case-studies" class="inline-flex items-center gap-2 rounded-md border border-line px-6 py-3 font-medium text-ink hover:border-signal">
            See the results
          </a>
        </div>
      </div>

      <dl class="grid grid-cols-2 gap-4">
        <div class="rounded-lg border border-line bg-white p-5">
          <dt class="text-xs font-medium uppercase tracking-wide text-slate">Avg. site launch time</dt>
          <dd class="mt-1 font-display text-3xl font-semibold text-signal">3-4 wks</dd>
        </div>
        <div class="rounded-lg border border-line bg-white p-5">
          <dt class="text-xs font-medium uppercase tracking-wide text-slate">Avg. increase in monthly leads</dt>
          <dd class="mt-1 font-display text-3xl font-semibold text-signal">+87%</dd>
        </div>
        <div class="rounded-lg border border-line bg-white p-5">
          <dt class="text-xs font-medium uppercase tracking-wide text-slate">Sites moved to "good" CWV</dt>
          <dd class="mt-1 font-display text-3xl font-semibold text-signal">94%</dd>
        </div>
        <div class="rounded-lg border border-line bg-white p-5">
          <dt class="text-xs font-medium uppercase tracking-wide text-slate">Client retention (12mo)</dt>
          <dd class="mt-1 font-display text-3xl font-semibold text-signal">89%</dd>
        </div>
      </dl>
    </div>
  </section>

  <section aria-labelledby="services-heading" class="mx-auto max-w-6xl px-6 py-20">
    <div class="max-w-2xl">
      <h2 id="services-heading" class="text-3xl font-semibold text-ink sm:text-4xl">Two core services. Everything else supports them.</h2>
      <p class="mt-4 text-lg text-slate">
        A site that's built well but never found doesn't generate leads. A search strategy with nothing to point traffic at doesn't either. We do both, together, on purpose.
      </p>
    </div>

    {pillars.map((pillar) => (
      <div class="mt-12">
        <h3 class="text-xl font-semibold text-ink">{pillar.name}</h3>
        <ul class="mt-6 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {pillar.services.map((service) => (
            <li>
              <ServiceCard
                name={service.data.name}
                shortDescription={service.data.shortDescription}
                href={`/services/${service.id}`}
                icon={service.data.icon}
              />
            </li>
          ))}
        </ul>
      </div>
    ))}
  </section>

  <section aria-labelledby="proof-heading" class="border-y border-line bg-paper-dim">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <h2 id="proof-heading" class="text-3xl font-semibold text-ink sm:text-4xl">Proof, not adjectives.</h2>
      <div class="mt-10 grid gap-6 sm:grid-cols-3">
        <StatCard value="3 wks" label="From kickoff to launch" detail="Local service business, full site + on-page SEO" />
        <StatCard value="164%" label="Map pack impressions" detail="22-location retail client, 6 months" />
        <StatCard value="0 -> 34%" label="AI Overview share of answer" detail="D2C e-commerce brand, 9 months" />
      </div>
    </div>
  </section>

  {featuredStudies.length > 0 && (
    <section aria-labelledby="case-studies-heading" class="mx-auto max-w-6xl px-6 py-20">
      <div class="flex flex-wrap items-end justify-between gap-4">
        <h2 id="case-studies-heading" class="text-3xl font-semibold text-ink sm:text-4xl">Recent results</h2>
        <a href="/case-studies" class="inline-flex items-center gap-1 font-medium text-signal hover:underline">
          View all case studies
          <Icon name="arrow-right" class="h-4 w-4" />
        </a>
      </div>
      <ul class="mt-8 space-y-4">
        {featuredStudies.map((study) => (
          <li>
            <CaseStudyCard
              clientLabel={study.data.clientName}
              industry={study.data.industry}
              summary={study.data.summary}
              href={`/case-studies/${study.id}`}
              headlineMetric={{ value: study.data.metrics[0].value, label: study.data.metrics[0].label }}
            />
          </li>
        ))}
      </ul>
    </section>
  )}

  <Cta
    heading="Your craft is the hard part. Let us handle the rest."
    body="A free review of your current site (or a plan for your first one) and what it would take to turn it into a real source of leads."
  />
</BaseLayout>
